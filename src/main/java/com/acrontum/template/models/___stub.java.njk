{%-set camel_case_operation = _.camelCase(_.deburr(operation_name)).replace(' ','')-%}
{%-set pascal_case_operation = ucFirst(camel_case_operation)-%}
{%-set snake_case_operation = _.snakeCase(camel_case_operation).toUpperCase() -%}
package com.acrontum.template.models;

import java.time.LocalDateTime;
import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.GenerationType;
import javax.persistence.Id;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

/**
 * The type {{ pascal_case_operation }}.
 */
@Entity(name = "{{ snake_case_operation }}S")
@Data
@Builder(toBuilder = true)
@NoArgsConstructor
@AllArgsConstructor
public class {{ pascal_case_operation }} {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "id")
    private int id;

    @Column(name = "name")
    private String name;

    @Column(name = "deleted_at")
    private LocalDateTime deletedAt;
}